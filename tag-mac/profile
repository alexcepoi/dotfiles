#! /usr/bin/env bash

# Load any common login shell settings if present.
[ -r ~/.sh_profile ]  && source ~/.sh_profile

# All login shells inherit configuration of interactive shells.
[ -r ~/.bashrc ]  && source ~/.bashrc
