# general
set -g default-terminal 'screen-256color'
set -g update-environment ''
set -g mouse off

setw -g xterm-keys on
setw -g monitor-activity on

# settings
set -g base-index 1
set -g renumber-windows on
set -g set-titles on
set -g set-titles-string '#S'
setw -g automatic-rename on
setw -g automatic-rename-format "#T"
setw -g aggressive-resize on
set -g display-panes-time 1000

set -g history-limit 100000
set -sg escape-time 0

set -g status-keys emacs
set -g mode-keys vi

# status
set -g status-fg 'colour74'
set -g status-bg 'colour238'
set -g status-left '#[fg=colour236,bg=colour74] #S '
set -g status-right '[#F] #[fg=colour236,bg=colour74] #h '

set -g pane-active-border-style 'fg=red,bg=colour234'
set -g pane-border-style 'fg=colour243,bg=colour235'
set -g window-active-style 'bg=colour234'
set -g window-style 'bg=colour235'

setw -g window-status-activity-fg 'colour74'
setw -g window-status-activity-bg 'colour238'
setw -g window-status-activity-attr bold

setw -g window-status-separator ' '
setw -g window-status-format ' #I:#[fg=colour249]#W '
setw -g window-status-current-format '#[bg=colour124] #I:#[fg=colour15]#W '

# default keybindings are great!
unbind -a
set -g prefix 'M-\'
bind \ send-prefix

bind : command-prompt
bind ? list-keys
bind d detach
bind D choose-client
bind e showenv -g
bind r attach -d
bind R source-file ~/.tmux.conf \; display-message 'Configuration reloaded.'
bind '~' show-messages

bind -n M-Enter new-window -c '#{pane_current_path}'
bind c new-window
bind w choose-window
bind , command-prompt -I '#W' 'rename-window "%%"'
bind q confirm-before -p 'kill-window #W? (y/n)' kill-window
bind s choose-session
bind n command-prompt 'new-session -As "%%"'

bind -n M-1 select-window -t :0
bind -n M-1 select-window -t :1
bind -n M-2 select-window -t :2
bind -n M-3 select-window -t :3
bind -n M-4 select-window -t :4
bind -n M-5 select-window -t :5
bind -n M-6 select-window -t :6
bind -n M-7 select-window -t :7
bind -n M-8 select-window -t :8
bind -n M-9 select-window -t :9

bind -n M-p prev
bind -n M-n next
bind M-p swap-window -t -1
bind M-n swap-window -t +1

bind -n M-[ split-window -h -c '#{pane_current_path}'
bind -n M-] split-window -v -c '#{pane_current_path}'
bind [ select-layout even-horizontal
bind ] select-layout even-vertical
bind = select-layout tiled

bind -n M-` resize-pane -Z
bind ! break-pane
bind C-[ choose-window 'join-pane -dbh -s "%%"'
bind C-] choose-window 'join-pane -dbv -s "%%"'
bind x confirm-before -p "kill-pane #P? (y/n)" kill-pane
bind o display-panes
bind O command-prompt -I '#P' 'swap-pane -d -t "%%"'

bind -n M-Up select-pane -U
bind -n M-Down select-pane -D
bind -n M-Left select-pane -L
bind -n M-Right select-pane -R
bind -n M-k select-pane -U
bind -n M-j select-pane -D
bind -n M-h select-pane -L
bind -n M-l select-pane -R

bind Up resize-pane -U 5
bind Down resize-pane -D 5
bind Left resize-pane -L 5
bind Right resize-pane -R 5
bind k resize-pane -U 5
bind j resize-pane -D 5
bind h resize-pane -L 5
bind l resize-pane -R 5

# copy-mode quirks
bind -n C-Space copy-mode \; send '0'
bind -n C-PageUp copy-mode -u \; send '0'
bind -n C-PageDown copy-mode \; send PageDown \; send '0'
bind p paste-buffer
bind P choose-buffer

# default vi-copy bindings are great too!
unbind -T copy-mode-vi send-keys -X -a
bind -T copy-mode-vi send-keys -X      h cursor-left
bind -T copy-mode-vi send-keys -X      j cursor-down
bind -T copy-mode-vi send-keys -X      k cursor-up
bind -T copy-mode-vi send-keys -X      l cursor-right
bind -T copy-mode-vi send-keys -X    C-b page-up
bind -T copy-mode-vi send-keys -X    C-f page-down
bind -T copy-mode-vi send-keys -X    C-d halfpage-down
bind -T copy-mode-vi send-keys -X    C-u halfpage-up

bind -T copy-mode-vi send-keys -X     Up cursor-up
bind -T copy-mode-vi send-keys -X   Down cursor-down
bind -T copy-mode-vi send-keys -X   Left cursor-left
bind -T copy-mode-vi send-keys -X  Right cursor-right
bind -T copy-mode-vi send-keys -X  PageDown page-down
bind -T copy-mode-vi send-keys -X  PageUp page-up

bind -T copy-mode-vi send-keys -X      g history-top
bind -T copy-mode-vi send-keys -X      G history-bottom
bind -T copy-mode-vi send-keys -X      w next-word
bind -T copy-mode-vi send-keys -X      b previous-word
bind -T copy-mode-vi send-keys -X      e next-word-end
bind -T copy-mode-vi send-keys -X      W next-space
bind -T copy-mode-vi send-keys -X      B previous-space
bind -T copy-mode-vi send-keys -X      E next-space-end

bind -T copy-mode-vi send-keys -X      0 start-of-line
bind -T copy-mode-vi send-keys -X      ^ back-to-indentation
bind -T copy-mode-vi send-keys -X    '$' end-of-line
bind -T copy-mode-vi send-keys -X      1 start-number-prefix
bind -T copy-mode-vi send-keys -X      2 start-number-prefix
bind -T copy-mode-vi send-keys -X      3 start-number-prefix
bind -T copy-mode-vi send-keys -X      4 start-number-prefix
bind -T copy-mode-vi send-keys -X      5 start-number-prefix
bind -T copy-mode-vi send-keys -X      6 start-number-prefix
bind -T copy-mode-vi send-keys -X      7 start-number-prefix
bind -T copy-mode-vi send-keys -X      8 start-number-prefix
bind -T copy-mode-vi send-keys -X      9 start-number-prefix

bind -T copy-mode-vi send-keys -X      v begin-selection
bind -T copy-mode-vi send-keys -X    C-v rectangle-toggle
bind -T copy-mode-vi send-keys -X Escape cancel
bind -T copy-mode-vi send-keys -X  Enter clear-selection
bind -T copy-mode-vi send-keys -X      q cancel
bind -T copy-mode-vi send-keys -X      y copy-selection
bind -T copy-mode-vi send-keys -X      Y copy-end-of-line

bind -T copy-mode-vi send-keys -X      H top-line
bind -T copy-mode-vi send-keys -X      M middle-line
bind -T copy-mode-vi send-keys -X      L bottom-line

bind -T copy-mode-vi send-keys -X      : goto-line
bind -T copy-mode-vi send-keys -X      / search-backward
bind -T copy-mode-vi send-keys -X      n search-reverse
bind -T copy-mode-vi send-keys -X      N search-again
